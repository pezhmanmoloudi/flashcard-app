# authentication.md

Authentication Rules:

System:

* JWT authentication

Rules:

* validate tokens securely
* expire tokens properly
* never trust frontend user IDs
* protect authenticated endpoints

Authorization:

* verify ownership
* validate permissions
* deny unauthorized access

Passwords:

* secure password hashing
* never log passwords

Important:
Authentication and authorization are critical security boundaries.
